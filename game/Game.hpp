#ifndef GAME_HPP
#define GAME_HPP

namespace Game
{
    enum PlayerIdentifier : uint8_t
    {
        O = 0,
        X = 1
    };
    // TODO : Create classes to handle games between players.
} // namespace Game

#endif